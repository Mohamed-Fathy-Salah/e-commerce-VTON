apiVersion: apps/v1
kind: Deployment
metadata:
  name: garments-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: garments-mongo
  template:
    metadata:
      labels:
        app: garments-mongo
    spec:
      containers:
        - name: garments-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: garments-mongo-srv
spec:
  selector:
    app: garments-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
